import { Container } from "react-bootstrap";
import styles from "./page.module.css";

export default function Home() {
  return (
    <Container>
      <h1>Home</h1>
      {/* Here is were we map data from api and show them as cards. */}
    </Container>
  );
}
